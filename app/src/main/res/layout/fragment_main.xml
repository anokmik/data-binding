<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.anokmik.databinding.communicator.MainFragmentCommunicator" />

        <variable
            name="communicator"
            type="MainFragmentCommunicator" />

    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:padding="@dimen/layout_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button
                android:id="@+id/find_view_by_id"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[0]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/butter_knife"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[1]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/data_binding_model"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[2]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/data_binding_ids"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[3]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/login"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[4]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/observable_greeting"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[5]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/notify_greeting"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[6]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/notes"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[7]}"
                android:textAllCaps="false" />

            <Button
                android:id="@+id/customization"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{(v) -> communicator.replaceFragment(v.id)}"
                android:text="@{communicator.titles[8]}"
                android:textAllCaps="false" />

        </LinearLayout>

    </ScrollView>

</layout>