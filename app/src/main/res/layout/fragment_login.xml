<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="communicator"
            type="com.anokmik.databinding.communicator.LoginFragmentCommunicator" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/layout_padding">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="@string/hint_login"
            android:inputType="text"
            bind:textWatcher="@{communicator.loginTextWatcher}" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:enabled="@{communicator.isLoginValid}"
            android:hint="@string/hint_password"
            android:inputType="textPassword"
            bind:textWatcher="@{communicator.passwordTextWatcher}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:enabled="@{communicator.isLoginValid &amp; communicator.isPasswordValid}"
            android:text="@string/button_sign_in"
            bind:onClickListener="@{communicator.clickListener}" />

    </LinearLayout>

</layout>